import styled from "styled-components";

export const Render = styled.div`
  width: 100%;
  display: flex;
  flex-direction: row;
  gap: .4rem;

  > svg {
    height: .75rem;
    width: .75rem;
  }
`
